# With no values configured, the service prints each daily plan to its own log.
# Set these only after configuring a WhatsApp Content Template whose first variable is the workout message.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
WHATSAPP_TO=
TWILIO_CONTENT_SID=
PORT=3000
